void main() {
  // For Loop
  for (int i = 5; i >= 1; i--) {
    print(i);
  }

  // For In Loop
  var names = ["John", "Tina", "Steve"];
  for (var name in names) {
    print(name);
  }

  // While Loop
  var num = 5;
  while (num >= 1) {
    print(num);
    num--;
  }
}
